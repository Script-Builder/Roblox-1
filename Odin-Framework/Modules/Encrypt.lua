--[[
  Odin Encryption Module
  
  Version : 0.1
  Build__ : Boxbone
  Type___ : ModuleScript
  
  Description : 
  This is just sha1 encryption and will be changed later!
]]--

local Module = {};
local Fake = {};

Module.Encrypt = function(String,Context,Level)
  local ByteArray=wait();
end;

Module.Byte = function(String) --//Unfinished
  local Return = {};
  for Char in String:gmatch(".") do table.insert(result, string.byte(letter)) end --//Lolno.
end;
